1.
Desarrollar un programa en el que se pida al usuario un vectorde numeros enteros e indique en pantalla cuantos de dichos elementos son numeros impares. Para comprobar si un numero es impar se puede utilizar lacondicion( n % 2 ) == 1.

2.
Desarrollar un programa en el que se pida al usuario un vector de numeros enteros e indique en pantalla el numero de ocurrencias de elementos repetidos. Por ejemplo el vector [1,2,3,1,2,1] tiene tres ocurrencias de elementos repetidos (dos 1 y un 2)


3.
Cargue un array de enteros con 200 elementos y muestre por pantalla los pares. Utilice la función rand()

4.
Muestre por pantalla una simulación de un tablero de ajedrez. Las casillas blancas las representaremos por un carácter B y las negras por un caracter N. Utilice arrays de dos dimensiones.

5.
Almacena en un aray los 10 primeros números pares. Imprímelos cada uno en una línea.

6.
Leer 5 valores de un array de enteros y deducir cuál es la distancia que les separa del número mayor. 

7.
Hacer un programa que llene un array de 100 enteros de forma que los 50 primeros números sean pares y los 50 siguientes sean impares. Luego, mostrarlos por pantalla

8.
Hacer un programa que llene un array de 60 enteros con números aleatorios del 0 al 99, luego hacer que los muestre en pantalla y diga cuál es el elemento mayor y cuántas veces se repite.

9.
Escribe un programa que pida al usuario por pantalla dos arrays de 10 elementos. Multiplica los elementos situados en identica posición en ambos arrays y guarda el resultado en esa posición en un tercer array. (es decir multiplica el primer elemento del primer array por el primer elemento del segundo array, y guardalo en el primer elemento del tercer array. Multiplica el segundo elemento del primer array por el segundo elemento del segundo array y guardalo en el segundo elemento del tercer array. Multiplica el tercer elemento del primer array por el tercer elemento del segundo array y guardalo en el tercer elemento del tercer array. Multiplica el cuarto elemento del primer array por el cuarto elemento del segundo array y guardalo en el cuarto elemento del tercer array, y así sucesivamente). Imprime los contenidos de ese tercer array.

