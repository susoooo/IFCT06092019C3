1-Enviar el mensaje recibido de vuelta.

2-Adapta el ejercício de la calculadora remota, implementándola usando sockets TCP. 
	0 -> hago una suma
		me enviar el primer número
		me envia el segundo número
		devuelvo el resultado
	1 -> hago un factorial
		me envio el primer número
		devuelvo el resultado

3-Crear un cliente que se conecte con varios servidores web (p.e google.es, elprogreso.es, xunta.gal) y recoja su página principal. El protocolo que utiliza un servidor web para servir datos es HTTP.

4-Crear un cliente que se conecte a un servidor de correo y envíe un correo. Utilizar el protocolo SMTP.

5-Crear un cliente que se conecte a un servidor de ftp y trate de obtener algunos datos de él.
